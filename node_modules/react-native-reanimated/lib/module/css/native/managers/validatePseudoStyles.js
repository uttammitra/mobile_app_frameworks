'use strict';

export function validatePseudoStyles(_pseudoStylesBySelector, _defaultStyle, _componentName) {
  // Only iOS gates input on opacity, so there is nothing to check elsewhere.
}
//# sourceMappingURL=validatePseudoStyles.js.map